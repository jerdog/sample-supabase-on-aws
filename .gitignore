# Dependencies
node_modules/
pnpm-lock.yaml
package-lock.json
# function-deploy lockfile MUST be committed: its Docker build runs
# `pnpm install --frozen-lockfile`, which fails on a clean clone without it.
!app/function-deploy/pnpm-lock.yaml

# Build output
dist/
*.tsbuildinfo
infra/bin/*.js
infra/bin/*.d.ts
infra/lib/*.js
infra/lib/*.d.ts

# Configuration (contains sensitive deployment values)
config.json
cdk-outputs.json

# RDS CA bundle copied into per-service build contexts by build-and-push.sh
# (build-time artifact; the canonical copy lives at repo-root certs/)
app/*/certs/
app/*/*/certs/

# Environment variables
.env
.env.local
.env.*.local

# Logs
/logs/
*.log
npm-debug.log*
pnpm-debug.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Gradle
.gradle/

# Testing
coverage/
.nyc_output/

# Temporary files
tmp/
temp/

# Security scan results (contains sensitive findings)
scan-results/

# Documentation (local only)
doc/
# docs/ is local-only EXCEPT the published customer upgrade guide. Ignore the
# directory's contents (not the dir itself, so git can descend) then re-include
# the one published file.
docs/*
!docs/customer-upgrade-guide.html

# Exclude tenant-manager and IDE/AI configs
.cursor/
.kiro/
.claude/
CLAUDE.md
infra/cdk.context.json
infra/cdk.out/
infra/infra/cdk.out/
infra/application/
__pycache__/
*.pyc
